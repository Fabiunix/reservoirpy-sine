# reservoirpy-sine
Ejemplo simple: predecir una onda seno con ReservoirPy (Echo State Network).

## Requisitos
- Python 3.11 (recomendado)
- pip

## Instalación
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
